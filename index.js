// Root entry that re-exports the library from `src/`.
// This file is intentionally minimal so that tools like unpkg and
// bundlers can resolve the package entry point.
export * from './src/index.js';
